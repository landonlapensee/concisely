<div class="container video-container">
  <div class="d-flex">
    <div class="d-flex video-buttons">
      <button class="btn h-100 video-button-video" id="dailyTrigger">Start Video Chat</button>
      <% if @coach %>
        <button class="btn-secondary h-100 video-button-report"><%= link_to "Create Report", new_booking_report_path(params[:booking_id]) %></button>
      <% end %>
    </div>
    <div id="video-container"></div>
  </div>
</div>

