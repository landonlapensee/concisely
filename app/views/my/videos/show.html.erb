<div class="container">
  <div class="d-flex">
    <div class="d-flex flex-column justify-content-around video-buttons">
      <button class="btn h-100 my- mr-2 video-button" id="dailyTrigger">Start Video Chat</button>
      <% if @coach %>
        <button class="btn-secondary h-100 my-3 mr-2 video-button"><%= link_to "Create Report", new_booking_report_path(params[:booking_id]) %></button>
      <% end %>
    </div>
    <div id="video-container"></div>
  </div>
</div>

